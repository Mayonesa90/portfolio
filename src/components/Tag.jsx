export function Tag({title}){
    return(
        <div className=" self-end font-Lato  text-xs py-2 px-4 tracking-widest rounded-full bg-slate-600 hover:bg-black text-white w-fit">
            <p>{title}</p>
        </div>
    )

}